"use client";

import { RotateCcw, Search, Save } from "lucide-react";
import { CompanyMultiSelect } from "@/components/CompanyMultiSelect";
import { CompanySearch } from "@/components/company-search";
import { topics } from "@/lib/mock-data";
import type { SearchFilters as SearchFiltersType } from "@/lib/types";

const selectClass = "h-10 w-full rounded-md border border-slate-300 bg-white px-3 text-sm text-slate-800 shadow-sm shadow-slate-100 focus:border-blue-600";

export function SearchFilters({ filters, onChange, onSearch, onReset, onSave }: { filters: SearchFiltersType; onChange: (filters: SearchFiltersType) => void; onSearch: () => void; onReset: () => void; onSave: () => void }) {
  const update = <K extends keyof SearchFiltersType>(key: K, value: SearchFiltersType[K]) => onChange({ ...filters, [key]: value });
  return <section aria-label="검색 조건" className="border border-slate-200 bg-white"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-3"><h2 className="text-sm font-semibold text-slate-900">검색 조건</h2><span className="text-xs text-slate-500">공시 문서와 주석 주제를 선택하세요.</span></div><div className="grid grid-cols-1 gap-x-5 gap-y-4 p-5 lg:grid-cols-6"><div className="min-w-0"><label htmlFor="base-company-search" className="mb-1.5 block text-xs font-medium text-slate-700">기준기업</label>{filters.baseCompany ? <div className="flex min-h-10 items-center rounded-md border border-slate-300 bg-white px-2 py-1.5"><span className="inline-flex max-w-full items-center rounded bg-slate-100 px-2 py-1 text-xs font-medium text-slate-700"><span className="truncate">{filters.baseCompany.corpName} {filters.baseCompany.stockCode ? `(${filters.baseCompany.stockCode})` : ""}</span></span></div> : <div className="flex h-10 items-center rounded-md border border-dashed border-slate-300 bg-slate-50 px-3 text-xs text-slate-500">선택된 기준기업이 없습니다.</div>}<div className="mt-2"><CompanySearch id="base-company-search" compact placeholder="예: 삼성전자 또는 005930" excludeCorpCodes={filters.comparisonCompanies.map((company) => company.corpCode)} onSelect={(company) => update("baseCompany", company)} /></div></div><div className="lg:col-span-2"><span className="mb-1.5 block text-xs font-medium text-slate-700">비교기업</span><CompanyMultiSelect value={filters.comparisonCompanies} onChange={(companies) => update("comparisonCompanies", companies)} /></div><label className="block"><span className="mb-1.5 block text-xs font-medium text-slate-700">사업연도</span><select value={filters.year} onChange={(event) => update("year", event.target.value)} className={selectClass}><option>2025</option><option>2024</option><option>2023</option></select></label><label className="block"><span className="mb-1.5 block text-xs font-medium text-slate-700">보고서 유형</span><select value={filters.reportType} onChange={(event) => update("reportType", event.target.value as SearchFiltersType["reportType"])} className={selectClass}><option>사업보고서</option><option>반기보고서</option><option>분기보고서</option></select></label><label className="block"><span className="mb-1.5 block text-xs font-medium text-slate-700">주석 주제</span><select value={filters.topic} onChange={(event) => update("topic", event.target.value)} className={selectClass}>{topics.map((topic) => <option key={topic}>{topic}</option>)}</select></label></div><div className="flex flex-wrap items-center justify-end gap-2 border-t border-slate-100 px-5 py-3"><button onClick={onReset} className="inline-flex h-9 items-center gap-2 rounded-md border border-slate-300 bg-white px-3 text-sm font-medium text-slate-700 hover:bg-slate-50"><RotateCcw className="h-4 w-4" />조건 초기화</button><button onClick={onSave} className="inline-flex h-9 items-center gap-2 rounded-md border border-slate-300 bg-white px-3 text-sm font-medium text-slate-700 hover:bg-slate-50"><Save className="h-4 w-4" />검색조건 저장</button><button onClick={onSearch} className="inline-flex h-9 items-center gap-2 rounded-md bg-[#123c78] px-4 text-sm font-semibold text-white hover:bg-[#0e3162]"><Search className="h-4 w-4" />주석 검색</button></div></section>;
}
