import { Columns3, FoldVertical, Rows3 } from "lucide-react";
import type { ViewMode } from "@/lib/types";
import { cn } from "@/lib/utils";

export function SearchResultToolbar({ resultCount, year, viewMode, expanded, onViewModeChange, onExpandedChange }: { resultCount: number; year: string; viewMode: ViewMode; expanded: boolean; onViewModeChange: (mode: ViewMode) => void; onExpandedChange: (value: boolean) => void }) {
  return <div className="mb-4 flex flex-wrap items-center justify-between gap-3 border-b border-slate-200 pb-3"><p className="text-sm text-slate-700"><span className="font-semibold">{resultCount}개 기업</span> · {resultCount}개 사업보고서 · 관련 주석 {resultCount + 3}건 · {year}년 기준</p><div className="flex items-center gap-1"><button onClick={() => onViewModeChange("vertical")} className={cn("inline-flex h-8 items-center gap-1.5 rounded px-2.5 text-xs font-medium", viewMode === "vertical" ? "bg-slate-900 text-white" : "text-slate-600 hover:bg-slate-100")}><Rows3 className="h-3.5 w-3.5" />세로 보기</button><button onClick={() => onViewModeChange("side-by-side")} className={cn("inline-flex h-8 items-center gap-1.5 rounded px-2.5 text-xs font-medium", viewMode === "side-by-side" ? "bg-slate-900 text-white" : "text-slate-600 hover:bg-slate-100")}><Columns3 className="h-3.5 w-3.5" />나란히 비교</button><span className="mx-1 h-4 border-l border-slate-300" /><button onClick={() => onExpandedChange(!expanded)} className="inline-flex h-8 items-center gap-1.5 rounded px-2.5 text-xs font-medium text-slate-600 hover:bg-slate-100"><FoldVertical className="h-3.5 w-3.5" />전체 {expanded ? "접기" : "펼치기"}</button></div></div>;
}
